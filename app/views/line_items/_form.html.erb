<%= simple_form_for(@line_item) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.association :product %>
    <%= f.association :cart %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
