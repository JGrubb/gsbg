<!DOCTYPE html>
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
  <head>
  	<meta charset="utf-8" />
  	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->
  	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

  	<title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

  	<%= stylesheet_link_tag    "application" %>
  	<%= javascript_include_tag "modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header id="header">
    <% if current_user %>
      <div class="contain-to-grid">
        <nav class="top-bar">
          <ul class="title-area">
            <li class="name"></li>
          </ul>
        <% if current_user.is_admin %>
          <section class="top-bar-section">
          <ul class="right">
            <li><%= link_to "New show", new_show_path %></li>
          </ul>
          </section>
        <% end %>
        </nav>
      </div>
    <% end %>
      <div class="row">
        <div id="logo">
          <%= link_to content_tag(:h1, "Greensky Bluegrass", class: "column large-centered"), root_path %>
        </div>
      </div>
    </header>
    <% flash.each do |key, value| %>
      <div class='alert-box row <%= key %>' data-alert>
        <%= value %>
        <a href="#" class="close">&times;</a>
      </div>
    <% end %>

    <%= yield %>

    <footer id="footer">
      <nav class="row">
        <% if current_user %>
          <%= link_to "log out", destroy_user_session_path, data: { method: :delete } %>
        <% else %>
          <%= link_to "Log in", new_user_session_path %>
        <% end %>
      </nav>  
    </footer>
    <%= javascript_include_tag "application" %>
  </body>
</html>
